Place .po images here. If it needs a specific startup, you may need to prefix the file names with 01, 02, etc so sort order is maintained. And then edit disklist.sh with the associated startup key. For example PRODOS or LAUNCHER.SYSTEM. See DATA lines in disklist.sh found in the folder above. 

